

```bash
sudo nano /etc/systemd/system/media-converter.service
```

# 入力内容
```bash
# /etc/systemd/system/media-converter.service
[Unit]
Description=FFmpeg Media Converter API
After=network.target

[Service]
User=user
Group=user
WorkingDirectory=/var/www/media_conv
Environment="PATH=/var/www/media_conv/.venv/bin"
ExecStart=/var/www/media_conv/.venv/bin/uvicorn convert_server:app --host 0.0.0.0 --port 8004 --workers 1
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

# 再起動
```bash
sudo systemctl daemon-reload
sudo systemctl enable media-converter
sudo systemctl start media-converter
sudo systemctl status media-converter
```